# ==============================================================================
# Production / Environment Template
# ==============================================================================
# For production environments, keep the placeholder or inject via secure secret managers.
# For local development, copy this file to `.env` and supply your actual Gemini API key.
# NOTE: `.env` is ignored by git to protect credentials.

GEMINI_API_KEY=your-api-key

